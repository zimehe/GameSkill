<view class="home-page">
  <view class="hero">
    <view class="eyebrow">V2 GUIDEBOOK</view>
    <view class="title">云玩家游客图鉴</view>
    <view class="subtitle">多游戏图鉴工具书，按游戏归类入口，查完即走。</view>
  </view>

  <view class="search-card">
    <view class="search-icon">⌕</view>
    <input
      class="search-input"
      placeholder="搜游戏、精灵、技能、属性..."
      value="{{keyword}}"
      confirm-type="search"
      bindinput="handleSearchInput"
      bindconfirm="submitSearch"
    />
    <view class="search-action">搜索</view>
  </view>

  <view wx:if="{{loading}}" class="loading-state">正在读取攻略库...</view>
  <view wx:elif="{{errorMessage}}" class="empty-state">{{errorMessage}}</view>

  <block wx:else>
    <view class="section-head library-head">
      <view>
        <view class="section-title">游戏库</view>
        <view class="section-note">常用工具会显示在对应游戏下方</view>
      </view>
      <view wx:if="{{matchedGameCount > visibleGames.length || showAllGames}}" class="toggle-games" bindtap="toggleAllGames">
        {{showAllGames ? '收起' : '展开全部'}}
      </view>
    </view>

    <view wx:if="{{visibleGames.length === 0}}" class="empty-state">
      没找到对应游戏，换个关键词试试。
    </view>

    <view wx:else class="game-list">
      <view
        wx:for="{{visibleGames}}"
        wx:key="slug"
        class="game-tile"
      >
        <view class="game-main" data-slug="{{item.slug}}" bindtap="handleGameTap">
          <view class="tile-mark" style="background: {{item.safeThemeColor}};">{{item.gameInitial}}</view>
          <view class="tile-copy">
            <view class="tile-name">{{item.name}}</view>
            <view class="tile-subtitle">{{item.subtitle}}</view>
          </view>
          <view class="game-arrow">›</view>
        </view>

        <view wx:if="{{item.modules.length > 0}}" class="module-list">
          <view
            wx:for="{{item.modules}}"
            wx:for-item="module"
            wx:key="id"
            class="module-chip"
            data-url="{{module.url}}"
            catchtap="handleModuleTap"
          >
            <view class="module-mark">{{module.mark}}</view>
            <view class="module-copy">
              <view class="module-title">{{module.title}}</view>
              <view class="module-desc">{{module.desc}}</view>
            </view>
          </view>
        </view>
      </view>
    </view>

  </block>

  <view class="version-footer">GuideBook v1.0.0</view>

  <view wx:if="{{showBannerAd}}" class="ad-safe-space"></view>
  <view wx:if="{{showBannerAd}}" class="banner-ad-wrap">
    <ad unit-id="{{adUnitId}}" ad-type="banner"></ad>
  </view>
</view>
